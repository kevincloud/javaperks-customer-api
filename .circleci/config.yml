version: 2.1
orbs:
  aws-s3: circleci/aws-s3@1.0.11
jobs:
  build:
    working_directory: ~/javaperks-customer-api
    docker:
      - image: circleci/openjdk:11-jdk-stretch
    steps:
      - checkout
      - restore_cache:
          key: javaperks-customer-api-{{ checksum "pom.xml" }}
      - run: mvn package
      - save_cache:
          paths:
            - ~/.m2
          key: javaperks-customer-api-{{ checksum "pom.xml" }}
      - store_artifacts:
          path: target/javaperks-customer-api-0.2.6.jar
      - aws-s3/copy:
          from: target/javaperks-customer-api-0.2.6.jar
          to: 's3://jubican-public/jars/javaperks-customer-api-0.2.6.jar'
          arguments: |
            --acl public-read
